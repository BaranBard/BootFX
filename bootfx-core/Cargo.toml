[package]
name = "bootfx-core"
version = "0.1.0"
edition = "2021"
description = "Shared configuration and data models for BootFX components"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
