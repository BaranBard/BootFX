[package]
name = "boot-video-player"
version = "0.1.0"
edition = "2021"
description = "Graphical video player that continues from ASCII boot handoff point"

[dependencies]
anyhow = "1"
bootfx-core = { path = "../bootfx-core" }
